apiVersion: v1
kind: Pod
metadata:
  name: label-demo
  labels:
    environment: production
    app: nginx
spec:
  containers:
  - name: nginx
    image: redhatraining/hello-world-nginx
    ports:
    - containerPort: 80
